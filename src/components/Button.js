import React from "react";

const Button = (props) => {

  return (
    <div>
      <button>Highlights</button>
      <button>News</button>
      <button>Fan Favorites</button>

    </div>
  )
};

export default Button;